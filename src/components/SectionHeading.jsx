const SectionHeading = ({ title }) => {
  return (
    <div className="mb-8 md:mb-16 text-center">
      <h1 className="text-3xl md:text-5xl lg:text-7xl font-semibold text-gray-700">{title}</h1>
    </div>
  );
};

export default SectionHeading;
